# Integra-Magna-Project
# NFT Landing Page – CSS Grid Project

## 📌 Overview
This project is a fully responsive landing page built using **HTML and CSS Grid only** (no Flexbox or frameworks).  
The design replicates the provided reference layout, focusing on structure, responsiveness, and visual accuracy.

---

## 🧠 Approach Used

### 1️⃣ Layout Structure
The page layout was divided into three main grid sections:

- **Navbar**
- **Main container Section**
- **Footer Strip**

The overall page structure uses:

```css
display: grid;
grid-template-rows: auto 1fr auto;
